import styled from "styled-components";
const Container = styled.div`
  padding-top: 10rem;
`;
const Preferences = () => {
  return <Container>Preferences</Container>;
};

export default Preferences;
