import styled from "styled-components";
import { Box } from "../CardStyles";
const StyledBox = styled(Box)`
  max-width: 1300px;
  margin: 1rem auto;
`;

const LastMonth = () => {
  return <StyledBox>LastMonth</StyledBox>;
};

export default LastMonth;
